const state = {
  isLogged: true, // {Boolean} is user is logged (default true to don't flash LoginModal)
  isVerified: false, // {Boolean} user is logged and check auth
  history: [], // {Array} navigator history
}

export default state
